.. _configuration:

Configuration
=============

zRPC is configured partly via the devicetree, partly in a YAML configuration file. The former is
used to describe the :ref:`backend <concept_backend>` over which RPCs are to be sent whereas the
latter define the RPCs themselves.

.. toctree::
   :maxdepth: 2
   :caption: Contents:

   devicetree
   yaml/index
